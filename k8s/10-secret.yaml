apiVersion: v1
kind: Secret
metadata:
  name: openclaw-secrets
  namespace: openclaw
type: Opaque
stringData:
  # Injected via envsubst from CI/CD environment variable
  gateway-token: "${GATEWAY_TOKEN}"
